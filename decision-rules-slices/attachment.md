# Decision Rules — service-attachment

## Master Table

| Rule ID | Description | Inputs | Outputs | Source citation | Category |
|---------|-------------|--------|---------|-----------------|----------|
| BR-attachment-001 | File size must not exceed the soft limit of `maxattachmentsize` KB (default 1000 KB) | `fileSize`, `maxattachmentsize` | accept / reject(file_exceeds_soft_limit) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-1 | validation |
| BR-attachment-002 | File size must not exceed the hard limit of `maxlocalattachment` MB (default 50 MB) regardless of configuration | `fileSize`, `maxlocalattachment` | accept / reject(file_exceeds_hard_limit) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-2 | validation |
| BR-attachment-003 | MIME type must match the `LEGAL_CONTENT_TYPES` regex whitelist | `mimeType`, `LEGAL_CONTENT_TYPES` | accept / reject(illegal_mime_type) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-3 | validation |
| BR-attachment-004 | If MIME is omitted or `application/octet-stream`, sniff binary content; if detected as `text/x-diff` or `text/x-patch`, auto-set `isPatch=true` and override `mimeType` to `text/plain` | `mimeType`, `binary data` | derive(isPatch=true, mimeType=text/plain) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-4 | business-policy |
| BR-attachment-005 | Filename must be stripped to basename only and truncated from the end to `MAX_ATTACH_FILENAME_LENGTH` characters | `filename`, `MAX_ATTACH_FILENAME_LENGTH` | derive(sanitized filename) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-5 | validation |
| BR-attachment-006 | When `isPatch=true`, content type is forced to `text/plain` regardless of provided or detected MIME | `isPatch`, `mimeType` | derive(mimeType=text/plain) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-6 | business-policy |
| BR-attachment-007 | When `isObsolete` transitions to `true`, every flag instance with `status='?'` is canceled (emits `AttachmentFlagCleared` per flag with `reason='obsolete-cascade'`); flags with status `+` or `-` are unaffected | `isObsolete`, flag instances with `status='?'` | cascade(clear pending flags) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-7 | state-transition |
| BR-attachment-008 | A deleted attachment cannot receive any further modifications; all commands except queries must return an error | `attachment.isDeleted` | reject(attachment_deleted) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-8 | business-policy |
| BR-attachment-009 | On deletion, all flags are cleared, `isObsolete=true`, `mimeType=text/plain`, `isPatch=false`, and binary data is removed from object storage | `attachmentId` | derive(isObsolete=true, mimeType=text/plain, isPatch=false, binary removed) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-9 | state-transition |
| BR-attachment-010 | A flag type is available for a target only when the target's product/component matches at least one inclusion scope (with null = wildcard) and does not match any exclusion scope; no inclusions = not available | `target.productId`, `target.componentId`, `flagType.inclusions`, `flagType.exclusions` | accept / reject(flag_type_not_applicable) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-10 | business-policy |
| BR-attachment-011 | A flag type with `isActive=false` cannot have new flag instances created; existing flags remain unchanged | `flagType.isActive` | reject(flag_type_inactive) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-11 | business-policy |
| BR-attachment-012 | A flag type's `targetType` must match the operation: `bug` for `SetBugFlag`, `attachment` for `SetAttachmentFlag`; cross-target usage is rejected | `flagType.targetType`, command type | accept / reject(target_type_mismatch) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-12 | validation |
| BR-attachment-013 | If `isMultiplicable=false`, at most one flag of that type per target; a new `?` request updates the existing flag rather than creating a duplicate | `flagType.isMultiplicable`, existing flag instances | accept(update existing) / reject(multiplicable_violation) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-13 | business-policy |
| BR-attachment-014 | On re-request (`?` → `?` with different requestee), the `setterId` is NOT updated to preserve the original requester for notification routing | `status`, `previousStatus`, `setterId` | derive(setterId unchanged) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-14 | business-policy |
| BR-attachment-015 | Setting `?` requires `requestGroupId` membership; setting `+`/`-` requires `grantGroupId` membership; clearing (`X`) allowed for original setter or any grant/request group member; null group = anyone | `user groups`, `flagType.grantGroupId`, `flagType.requestGroupId`, `flag.setterId`, requested status | accept / reject(insufficient_group_membership) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-15 | permission |
| BR-attachment-016 | A specified `requesteeId` must have an enabled account, be able to see the bug, see the attachment if private, and have grant group permission; if `skipRequesteeOnError`, silently drop the requestee on failure | `requesteeId`, requestee account status, bug visibility, attachment privacy, grant group | accept / reject(requestee_invalid) or derive(requesteeId dropped) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-16 | validation |
| BR-attachment-017 | Setting `isPrivate=true` or editing an already-private attachment requires insider group membership for the bug's product | `isPrivate`, user insider group membership, bug product | accept / reject(not_insider) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-17 | permission |
| BR-attachment-018 | On bug product/component change, all flags on the bug and its attachments are checked against new product/component; invalid flags are retargeted to a same-name applicable flag type or cleared with `reason='retarget'` | `BugProductChanged`, all flag instances, flag type applicability rules | cascade(retarget or clear flags) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-18 | business-policy |
| BR-attachment-019 | On flag type inclusion/exclusion rule change, existing flag instances of that type are checked; flags no longer valid are cleared with `reason='retarget'` | `FlagTypeInclusionsChanged`/`FlagTypeExclusionsChanged`, existing flag instances | cascade(clear invalid flags) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-19 | business-policy |
| BR-attachment-020 | Flag type name must not contain spaces or commas and must be unique across all flag types | `name` | accept / reject(name_format_violation or name_not_unique) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#business-rule-20 | validation |
| BR-attachment-PERM-CreateAttachment | `attachments:create` permission required to create an attachment | caller JWT permissions | accept / reject(forbidden) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-1 | permission |
| BR-attachment-PERM-UpdateAttachmentMetadata | `attachments:update` permission required to update attachment metadata | caller JWT permissions | accept / reject(forbidden) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-1 | permission |
| BR-attachment-PERM-DeleteAttachment | `attachments:delete` permission required to delete an attachment | caller JWT permissions | accept / reject(forbidden) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-1 | permission |
| BR-attachment-PERM-SetAttachmentFlagRequest | `flags:request` permission required to set a flag to `?` status | caller JWT permissions, requested status | accept / reject(forbidden) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-1 | permission |
| BR-attachment-PERM-SetAttachmentFlagSet | `flags:set` permission required to set a flag to `+`/`-`/`X` status | caller JWT permissions, requested status | accept / reject(forbidden) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-1 | permission |
| BR-attachment-PERM-CreateFlagType | `flag-types:create` permission required to create a flag type definition | caller JWT permissions | accept / reject(forbidden) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-1 | permission |
| BR-attachment-PERM-UpdateFlagType | `flag-types:update` permission required to update or deactivate a flag type | caller JWT permissions | accept / reject(forbidden) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-1 | permission |
| BR-attachment-POLICY-CanEditAttachmentPolicy | User must be the attachment's `submitterId` or have `editbugs` group membership for the bug's product; for private attachments, user must additionally be in the insider group | `submitterId`, `userId`, `editbugs` group, insider group, bug product | accept / reject(cannot_edit_attachment) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-2 | business-policy |
| BR-attachment-POLICY-CanEditPrivateAttachmentPolicy | When `isPrivate=true`, user must be in the insider group for the bug's product | `isPrivate`, user insider group membership, bug product | accept / reject(not_insider) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-2 | business-policy |
| BR-attachment-POLICY-CanSetFlagPolicy | Validates group membership per requested status: `?` → `requestGroupId`, `+`/`-` → `grantGroupId`, `X` → original setter or grant/request group; null group = anyone | `user groups`, `flagType.grantGroupId`, `flagType.requestGroupId`, `flag.setterId`, requested status | accept / reject(insufficient_group_membership) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-2 | business-policy |
| BR-attachment-POLICY-FlagTypeApplicabilityPolicy | Flag type must be applicable to the target's product/component per inclusion/exclusion rules, must be `isActive=true`, and `targetType` must match the command | `flagType.isActive`, `flagType.targetType`, `flagType.inclusions`, `flagType.exclusions`, target product/component | accept / reject(flag_type_not_applicable) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-2 | business-policy |
| BR-attachment-POLICY-RequesteeVisibilityPolicy | Requestee must have an enabled account, see the bug, see the attachment if private, and have grant group permission | `requesteeId`, requestee account, bug visibility, attachment privacy, grant group | accept / reject(requestee_invalid) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-2 | business-policy |
| BR-attachment-POLICY-MultiplicableFlagPolicy | If `isMultiplicable=false` and a flag of the same type already exists on the target, the existing flag is updated rather than creating a duplicate | `flagType.isMultiplicable`, existing flag instances of same type on target | accept(update existing) / accept(create new) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#authorization-layer-2 | business-policy |
| BR-attachment-ST-None-Active | Attachment created in Active state when user has `attachments:create` and bug exists | `bugId`, caller JWT | derive(attachment=Active) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-Active-Obsolete | Attachment transitions to Obsolete, cascading cancellation of all pending `?` flags | `attachmentId`, user is submitter or has `attachments:update` | derive(isObsolete=true, pending flags cleared) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-Active-Deleted | Attachment transitions directly from Active to Deleted | `attachmentId`, user is submitter or has `attachments:delete` | derive(attachment=Deleted, binary removed) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-Obsolete-Deleted | Obsolete attachment transitions to Deleted | `attachmentId`, user is submitter or has `attachments:delete` | derive(attachment=Deleted, binary removed) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-FlagNone-Requested | Flag instance created in `?` (requested) state when user is in `requestGroupId` and flag type is active and applicable | `flagTypeId`, user groups, flag type applicability | derive(flag.status=?') | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-FlagRequested-Granted | Flag transitions from `?` to `+` when user is in `grantGroupId` | flag instance, user groups | derive(flag.status='+') | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-FlagRequested-Denied | Flag transitions from `?` to `-` when user is in `grantGroupId` | flag instance, user groups | derive(flag.status='-') | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-FlagRequested-Cleared | Flag transitions from `?` to deleted via user clear (`X`) or system cascade (obsolete/retarget) | flag instance, user or system trigger | derive(flag=DELETED) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-FlagGranted-Requested | Granted flag (`+`) can be re-requested (`?`) by a user in `requestGroupId`; setter is updated | flag instance, user groups | derive(flag.status='?') | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-FlagDenied-Requested | Denied flag (`-`) can be re-requested (`?`) by a user in `requestGroupId`; setter is updated | flag instance, user groups | derive(flag.status='?') | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-FlagTypeNone-Active | Flag type created in Active state when user has `flag-types:create` | caller JWT | derive(flagType=Active) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-ST-FlagTypeActive-Inactive | Flag type transitions to terminal Inactive state; prevents new flag creation but preserves existing flags | `flagTypeId`, user has `flag-types:update` | derive(flagType.isActive=false) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#state-machines | state-transition |
| BR-attachment-NOTIF-AttachmentFlagRequested | Notify the requestee when a flag is set to `?` on an attachment | `AttachmentFlagRequested` event, `requesteeId` | notify(requestee via email) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-AttachmentFlagGranted | Notify the original setter when a flag is granted (`+`) on an attachment | `AttachmentFlagGranted` event, `setterId` | notify(setter via email) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-AttachmentFlagDenied | Notify the original setter when a flag is denied (`-`) on an attachment | `AttachmentFlagDenied` event, `setterId` | notify(setter via email) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-AttachmentFlagCleared | Notify setter/requestee when a flag is cleared on an attachment, routing depends on reason | `AttachmentFlagCleared` event, `reason`, `setterId`, `requesteeId` | notify(setter or requestee via email) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-BugFlagRequested | Notify the requestee when a bug-level flag is set to `?` | `BugFlagRequested` event, `requesteeId` | notify(requestee via email) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-BugFlagGranted | Notify the setter when a bug-level flag is granted (`+`) | `BugFlagGranted` event, `setterId` | notify(setter via email) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-BugFlagDenied | Notify the setter when a bug-level flag is denied (`-`) | `BugFlagDenied` event, `setterId` | notify(setter via email) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-BugFlagCleared | Notify setter when a bug-level flag is cleared | `BugFlagCleared` event, `setterId` | notify(setter via email) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-AttachmentCreated | Notify subscribers (service-comment, service-notification, service-search) when an attachment is created | `AttachmentCreated` event | notify(service-comment, service-notification, service-search) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-AttachmentMarkedObsolete | Notify subscribers (service-comment, service-notification, service-search) when an attachment is marked obsolete | `AttachmentMarkedObsolete` event | notify(service-comment, service-notification, service-search) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
| BR-attachment-NOTIF-AttachmentDeleted | Notify subscribers (service-notification, service-search) when an attachment is deleted | `AttachmentDeleted` event | notify(service-notification, service-search) | output/phase-5-specification/specs/service-attachment/SERVICE_SPEC.md#cross-service-integration | notification |
